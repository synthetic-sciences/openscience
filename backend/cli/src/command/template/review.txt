Run an independent, read-only review of this scope:

$ARGUMENTS

If the scope is empty, review the current session's work: its plan, changed files, generated artifacts, claims, citations, figures, and provenance records. A scope may be a file, artifact, claim, commit, branch, pull request, or result.

Resolve code targets explicitly. With no code scope, inspect `git status --short`, unstaged and staged diffs, and every untracked file. For a commit, inspect `git show`; for a branch, compare its merge base with HEAD; for a pull request, read its metadata, full diff, reviews, and checks. Then read the complete affected files and repository instructions before judging the change.

Work from observable evidence. Trace scientific claims and numbers to exact sources, artifacts, or provenance nodes. Check methods, statistics, units, data leakage, reproducibility, error handling, cancellation, and repository instructions where relevant. Do not claim to have rerun anything you did not run.

Report only high-signal findings. For each finding give severity, exact target, evidence, impact, and the smallest credible fix. Distinguish confirmed defects from limitations or unverified risks. Record provenance review findings when an exact target exists. If nothing material is wrong, say what was checked and state that no blocking findings were found.

Workspace: ${path}
