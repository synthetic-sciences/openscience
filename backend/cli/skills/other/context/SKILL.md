---
name: context
description: Show the current conversation context composition, estimated capacity, and compaction state. Use when the user invokes /context or asks how much context remains.
---

# Show context usage

Report the deterministic session estimate, including message composition, model capacity when available, and prior compactions. Do not spend a model turn or mutate the session.
